version: 0.2
phases:
  build:
    commands:
      - echo "Construyendo imagen"
      - docker build -t flask-app ./app
  post_build:
    commands:
      - echo "Subiendo a S3"
      - aws s3 cp --recursive ./app s3://app-flask/
artifacts:
  files:
    - '**/*'
